import Link from 'next/link'

export const dynamic = 'force-dynamic'

import { getProjects } from '@/lib/projects'
export default async function Home(){const projects=await getProjects(); return <><header className="wrap nav"><Link href="/" className="mono">LARAIB<span style={{color:'var(--acid)'}}>.</span></Link><nav className="navlinks"><a href="#work">Work</a><a href="#about">About</a><Link href="/admin">Admin</Link></nav><a className="button primary" href="mailto:hello@laraib.dev">Let&apos;s talk</a></header><main><section className="wrap hero"><div><div className="eyebrow mono">Product engineer / designer</div><h1>Digital work with a point of view.</h1><p>I build thoughtful interfaces and dependable systems for products people love to use.</p><div className="row" style={{marginTop:28}}><a className="button primary" href="#work">See selected work</a><a className="button" href="mailto:hello@laraib.dev">Get in touch</a></div></div><div className="hero-side mono">Based in Lahore<br/>Working globally<br/><br/>Available for select projects</div></section><section id="work" className="wrap section"><div className="section-head"><div><div className="eyebrow mono">Selected work</div><h2>Things I&apos;ve shipped.</h2></div><span className="muted mono">{projects.length.toString().padStart(2,'0')} projects</span></div><div className="grid">{projects.map((p)=><article className="card" key={p.id}><div className="eyebrow mono">{p.featured?'Featured project':'Case study'}</div><h3>{p.title}</h3><p>{p.description}</p><div className="tags">{p.tags.map(tag=><span className="tag" key={tag}>{tag}</span>)}</div><div className="row" style={{marginTop:20}}>{p.projectUrl&&<a className="button" href={p.projectUrl} target="_blank">View project ↗</a>}{p.githubUrl&&<a className="button" href={p.githubUrl} target="_blank">Source ↗</a>}</div></article>)}</div>{projects.length===0&&<div className="card"><p>No projects published yet. Add your first one from the admin panel.</p></div>}</section><section id="about" className="wrap section"><div className="card"><div className="eyebrow mono">A little about me</div><h2>Curious by default.</h2><p className="muted">I work across product strategy, interaction design, and full-stack engineering to turn ambiguous ideas into useful, durable products.</p></div></section></main><footer className="wrap footer">© 2026 Laraib Abbasi. Built with care.</footer></>}
